runtime: "container"
startCommand:
  type: "mcp"
  configSchema:
    type: "object"
    properties:
      apiKey:
        type: "string"
        description: "Your API key for Kirha Tool Planning"
    required: ["apiKey"]
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."